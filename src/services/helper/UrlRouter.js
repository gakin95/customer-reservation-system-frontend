const loginUrl = "/users/login";
const users = "/users";
const venues = "/venues/";
const availabilty = "/availability";
const reservation = "/reservations";
const payment = "/make-payment";
const ratings = "/reviews"

const UrlRoute = {
  loginUrl,
  users,
  venues,
  availabilty,
  reservation,
  payment,
  ratings
};

export default UrlRoute;
